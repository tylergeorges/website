import { Ascii } from '@/components/ascii';

export const SiteLogo = () => (
  <code className="mt-4   text-[4vw] overflow-hidden text-secondary vertical  md:text-base">
    <Ascii ascii="circles" />
  </code>
);
